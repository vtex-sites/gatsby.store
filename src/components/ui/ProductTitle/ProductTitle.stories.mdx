import { Meta, Canvas, Story, ArgsTable } from '@storybook/addon-docs'
import ProductTitle from '.'
import { DiscountBadge } from 'src/components/ui/Badge'

<Meta
  title="Molecules/ProductTitle"
  component={ProductTitle}
  argTypes={{
    title: {
      control: { type: 'radio' },
      options: ['h1', 'h2', 'h3'],
      mapping: {
        h1: <h1>Apple Magic Mouse</h1>,
        h2: <h2>Apple Magic Mouse</h2>,
        h3: <h3>Apple Magic Mouse</h3>,
      },
      defaultValue: 'h1',
    },
    label: {
      control: { type: 'radio' },
      options: ['low', 'medium', 'hight'],
      mapping: {
        low: <DiscountBadge listPrice={100} spotPrice={90} big />,
        medium: <DiscountBadge listPrice={100} spotPrice={70} big />,
        hight: <DiscountBadge listPrice={100} spotPrice={10} big />,
      },
      defaultValue: 'low',
    },
    refNumber: {
      defaultValue: '99995945',
    },
  }}
/>

export const Template = (args) => <ProductTitle {...args} />

<header>

# ProductTitle

The product title that is commonly used on Product Details Page (PDP).

</header>

### Import

`import ProductTitle from '../components/ui/ProductTitle'`

### Usage

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>

<ArgsTable story="Default" />

| Local token                                 | Default value/Global token linked          |
| ------------------------------------------- | ------------------------------------------ |
| <code>--fs-product-title-font-size</code>   | <code>var(--fs-text-size-4)</code>         |
| <code>--fs-product-title-font-weight</code> | <code>var(--fs-text-weight-regular)</code> |
| <code>--fs-product-title-line-height</code> | <code>1.12</code>                          |

## Nested Elements

### Ref Number

| Local token                                           | Default value/Global token linked                                                                   |
| ----------------------------------------------------- | --------------------------------------------------------------------------------------------------- |
| <code>--fs-product-title-ref-number-margin-top</code> | <code>var(--fs-spacing-3)</code>                                                                    |
| <code>--fs-product-title-ref-number-color</code>      | <div style={{ backgroundColor: '--fs-color-text-light' }} /><code>var(--fs-color-text-light)</code> |
